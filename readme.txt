advantages:
1. mloc, flexible hierchical indexing using dictionary.
2. ['a',None,['b','c']] returns a MulSeries or raises error
3. md.mean(axis=) returns a MulSeries with the name being the funciton name 'mean'.